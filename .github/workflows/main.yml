name: Keep Alive

on:
  schedule:
    - cron: '*/20 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Assistant
        run: curl -s https://health-assisstant.onrender.com > /dev/null
